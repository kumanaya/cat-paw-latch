{
  "name": "@domo/native-wincred",
  "version": "0.1.0",
  "private": true,
  "description": "One generic credential in the Windows Credential Manager, via CredRead/CredWrite. The vault master key's most durable home on Windows; everything else about the vault lives in @domo/device-core.",
  "main": "./index.cjs",
  "scripts": {
    "install": "node scripts/build.cjs"
  },
  "devDependencies": {
    "node-addon-api": "^8.3.0",
    "node-gyp": "^13.0.0"
  }
}
