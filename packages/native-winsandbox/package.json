{
  "name": "@domo/native-winsandbox",
  "version": "0.1.0",
  "private": true,
  "description": "Windows Job Objects for agent-run commands: kill-on-close plus a process cap per run. The seatbelt seat on Windows; everything else about execution lives in @domo/device-core.",
  "main": "./index.cjs",
  "scripts": {
    "install": "node scripts/build.cjs"
  },
  "devDependencies": {
    "node-addon-api": "^8.3.0",
    "node-gyp": "^13.0.0"
  }
}
