{
  "name": "@domo/native-fs",
  "version": "0.1.0",
  "private": true,
  "description": "Windows file ACL lockdown: replace a secret file's inherited DACL with an owner-only one. The chmod-0600 floor is advisory on NTFS; everything about secret files lives in @domo/device-core.",
  "main": "./index.cjs",
  "scripts": {
    "install": "node scripts/build.cjs"
  },
  "devDependencies": {
    "node-addon-api": "^8.3.0",
    "node-gyp": "^13.0.0"
  }
}
